# SQL Hero 🎮

**Интерактивная платформа для изучения MySQL с геймификацией**

SQL Hero — это Telegram WebApp для изучения SQL в игровой форме. Проходите уроки, выполняйте задания, зарабатывайте опыт, открывайте достижения и соревнуйтесь с другими в таблице лидеров!

---

## 🌟 Основные возможности

### 📚 Обучение
- **10 модулей** от основ до продвинутых тем
- **100+ уроков** с теорией и практикой
- **Интерактивный SQL-редактор** с подсветкой синтаксиса
- **Песочница** для безопасного выполнения SQL-запросов
- **Мгновенная проверка** правильности решений

### 🎮 Геймификация
- **Система опыта (XP)** за прохождение уроков
- **Уровни (1-100)** с прогрессивным ростом
- **50+ достижений** за различные успехи
- **Серии дней** (streak) для мотивации
- **Таблица лидеров** для соревнования

### 📊 Прогресс и аналитика
- **Дашборд** с общей статистикой
- **Карта модулей** для визуализации прогресса
- **Тепловая карта активности** (GitHub-style)
- **Детальная статистика** по модулям и урокам

### 🔒 Безопасность
- **JWT аутентификация** через Telegram WebApp
- **Изолированная песочница** для выполнения SQL
- **Защита от SQL-инъекций** и опасных операций
- **Лимиты на выполнение** (timeout, количество строк)

---

## 🛠 Технологический стек

### Backend
- **FastAPI** 0.120+ — современный async веб-фреймворк
- **SQLAlchemy** 2.0+ — async ORM
- **MySQL** 8.0 — основная база данных
- **Alembic** — миграции базы данных
- **Pydantic** — валидация данных
- **PyJWT** — JWT токены
- **Uvicorn** — ASGI сервер
- **Poetry** — управление зависимостями

### Frontend
- **React** 19+ — UI библиотека
- **TypeScript** — типизированный JavaScript
- **Vite** 7+ — быстрый сборщик
- **React Router** — маршрутизация
- **Axios** — HTTP клиент
- **Telegram WebApp API** — интеграция с Telegram
- **pnpm** — управление зависимостями

### Infrastructure
- **Docker** + **Docker Compose** — контейнеризация
- **Nginx** — reverse proxy (production)
- **Git** + **pre-commit** — контроль версий

---

## 🚀 Быстрый старт

### Требования

- **Node.js** >= 18.0.0
- **Python** >= 3.11
- **MySQL** >= 8.0
- **pnpm** >= 8.0.0
- **Docker** и **Docker Compose** (рекомендуется)

### Установка через Docker (Рекомендуется)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/fedotrick/SQL-Hero.git
cd SQL-Hero

# 2. Настроить переменные окружения
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
# Отредактируйте .env файлы (см. инструкции ниже)

# 3. Запустить все сервисы
docker-compose up -d

# 4. Применить миграции и загрузить данные
docker-compose exec backend poetry run alembic upgrade head
docker-compose exec backend poetry run python -m app.cli.seed_data
```

**Проект будет доступен:**
- Frontend: http://localhost:5173
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs
- MySQL: localhost:3306

### Локальная установка (без Docker)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/fedotrick/SQL-Hero.git
cd SQL-Hero

# 2. Установить зависимости backend
cd backend
pip install poetry
poetry install
cd ..

# 3. Установить зависимости frontend
cd frontend
npm install -g pnpm
pnpm install
cd ..

# 4. Запустить MySQL (через Docker)
docker-compose up mysql -d

# 5. Настроить .env файлы
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
# Отредактируйте файлы

# 6. Применить миграции
cd backend
poetry run alembic upgrade head
poetry run python -m app.cli.seed_data
cd ..

# 7. Запустить backend
cd backend
poetry run uvicorn app.main:app --reload
# В отдельном терминале

# 8. Запустить frontend
cd frontend
pnpm dev
```

---

## ⚙️ Настройка переменных окружения

### Backend (.env)

```env
# Database
DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/sqlhero

# Telegram Bot (получить у @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_USERNAME=sql_hero_bot

# JWT Secret (сгенерировать случайную строку)
JWT_SECRET_KEY=super-secret-key-change-this-in-production-min-32-chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 дней

# Sandbox
SANDBOX_ENABLED=true
SANDBOX_TIMEOUT_SECONDS=5
SANDBOX_MAX_RESULT_ROWS=1000
SANDBOX_MYSQL_HOST=localhost
SANDBOX_MYSQL_PORT=3306
SANDBOX_MYSQL_ADMIN_USER=root
SANDBOX_MYSQL_ADMIN_PASSWORD=rootpassword

# CORS (добавьте ваш production домен)
CORS_ORIGINS=http://localhost:5173,https://your-domain.com

# Environment
ENVIRONMENT=development
```

### Frontend (.env)

```env
# API URL
VITE_API_BASE_URL=http://localhost:8000

# App Config
VITE_APP_NAME=SQL Hero
VITE_TELEGRAM_BOT_USERNAME=sql_hero_bot
```

---

## 📖 Документация

- **[SETUP.md](docs/SETUP.md)** — Полная инструкция по установке
- **[RUN_LOCAL.md](docs/RUN_LOCAL.md)** — Запуск для локальной разработки
- **[DOCKER.md](docs/DOCKER.md)** — Работа с Docker
- **[TELEGRAM_SETUP.md](docs/TELEGRAM_SETUP.md)** — Настройка Telegram Bot
- **[TESTING.md](docs/TESTING.md)** — Запуск тестов
- **[DEPLOYMENT.md](docs/DEPLOYMENT.md)** — Деплой в production
- **[TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)** — Решение проблем
- **[ARCHITECTURE.md](docs/ARCHITECTURE.md)** — Архитектура проекта
- **[CONTRIBUTING.md](docs/CONTRIBUTING.md)** — Как участвовать в разработке

---

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
poetry run pytest                    # Все тесты
poetry run pytest tests/e2e/ -v      # E2E тесты (SQLite, быстро)
poetry run pytest --cov=app          # С покрытием

# Frontend тесты
cd frontend
pnpm test                            # Unit тесты
pnpm test:coverage                   # С покрытием

# Линтинг
cd backend && poetry run ruff check app && poetry run mypy app
cd frontend && pnpm lint && pnpm type-check
```

---

## 📁 Структура проекта

```
SQL-Hero/
├── backend/                    # FastAPI backend
│   ├── app/
│   │   ├── core/              # Конфигурация, database, security
│   │   ├── models/            # SQLAlchemy модели
│   │   ├── routers/           # API endpoints
│   │   ├── services/          # Бизнес-логика
│   │   ├── schemas/           # Pydantic схемы
│   │   ├── cli/               # CLI команды (seed_data)
│   │   └── main.py            # Точка входа
│   ├── tests/                 # Тесты
│   │   ├── e2e/               # E2E тесты (SQLite)
│   │   └── test_*.py          # Unit/integration тесты
│   ├── alembic/               # Миграции базы данных
│   ├── pyproject.toml         # Poetry конфигурация
│   ├── Dockerfile             # Production образ
│   └── .env.example           # Пример переменных окружения
│
├── frontend/                   # React frontend
│   ├── src/
│   │   ├── pages/             # Страницы (Dashboard, Lesson, etc.)
│   │   ├── components/        # Переиспользуемые компоненты
│   │   ├── services/          # API клиенты
│   │   ├── hooks/             # Кастомные React хуки
│   │   ├── types/             # TypeScript типы
│   │   ├── utils/             # Утилиты
│   │   └── App.tsx            # Главный компонент
│   ├── public/                # Статические файлы
│   ├── package.json           # npm зависимости
│   ├── Dockerfile             # Production образ
│   ├── Dockerfile.dev         # Development образ
│   └── .env.example           # Пример переменных окружения
│
├── docs/                       # Документация
├── infrastructure/             # Инфраструктурные скрипты
├── docker-compose.yml          # Локальная разработка
├── .pre-commit-config.yaml     # Git хуки
└── README.md                   # Этот файл
```

---

## 🎯 Основные модули курса

1. **Введение в SQL** — Основы баз данных, SELECT
2. **Фильтрация данных** — WHERE, операторы сравнения
3. **Сортировка и группировка** — ORDER BY, GROUP BY, HAVING
4. **Функции** — Агрегатные и скалярные функции
5. **Объединение таблиц** — INNER JOIN, LEFT JOIN, RIGHT JOIN
6. **Подзапросы** — Вложенные SELECT запросы
7. **Изменение данных** — INSERT, UPDATE, DELETE
8. **Создание объектов** — CREATE TABLE, ALTER TABLE
9. **Индексы и оптимизация** — INDEX, EXPLAIN
10. **Продвинутые темы** — Транзакции, триггеры, представления

---

## 🤝 Участие в разработке

Мы приветствуем вклад сообщества! Вот как вы можете помочь:

1. **Fork** репозиторий
2. Создайте **feature branch**: `git checkout -b feature/amazing-feature`
3. **Commit** изменения: `git commit -m 'feat: Add amazing feature'`
4. **Push** в branch: `git push origin feature/amazing-feature`
5. Откройте **Pull Request**

Подробнее в [CONTRIBUTING.md](docs/CONTRIBUTING.md)

### Стиль коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/ru/):
- `feat:` — новая функция
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `refactor:` — рефакторинг кода
- `test:` — добавление тестов
- `chore:` — обновление конфигураций

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE).

---

## 📞 Контакты и поддержка

- **Issues:** [GitHub Issues](https://github.com/fedotrick/SQL-Hero/issues)
- **Telegram:** [@fedotrick](https://t.me/fedotrick)
- **Email:** support@sqlhero.dev (если настроен)

---

## 🙏 Благодарности

- Спасибо всем контрибьюторам проекта
- Вдохновлено лучшими практиками обучения программированию
- Построено с использованием открытых технологий

---

**Учитесь SQL в удовольствие! 🚀📊**
