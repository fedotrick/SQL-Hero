# Настройка Telegram Bot для SQL Hero

Полное руководство по созданию и настройке Telegram бота для SQL Hero.

---

## Обзор

SQL Hero использует Telegram WebApp API для запуска приложения внутри Telegram. Для этого необходимо:

1. Создать Telegram бота через @BotFather
2. Получить Bot Token
3. Настроить Web App Menu Button
4. Настроить домен (для production)
5. Настроить описание и изображения

---

## Шаг 1: Создание бота

### 1.1 Открыть BotFather

1. Откройте Telegram
2. Найдите официального бота **[@BotFather](https://t.me/BotFather)**
3. Нажмите "Start" или отправьте `/start`

### 1.2 Создать нового бота

Отправьте команду:
```
/newbot
```

BotFather попросит вас ввести:

**Название бота (display name):**
```
SQL Hero
```
или
```
SQL Hero Dev Bot  (для разработки)
```

**Username бота (должен заканчиваться на `bot`):**
```
sql_hero_bot
```
или
```
sql_hero_dev_bot  (для разработки)
```

**❗ Важно:** Username должен быть уникальным. Если занят, попробуйте другой вариант:
- `sqlhero_learning_bot`
- `my_sql_hero_bot`
- `sql_hero_2024_bot`

### 1.3 Получить токен

После создания бота BotFather отправит сообщение с **Bot Token**:

```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567
```

**⚠️ ВАЖНО:** 
- Храните токен в секрете!
- Не коммитьте в Git!
- Используйте `.env` файлы

### 1.4 Сохранить токен в .env

Откройте `backend/.env` и добавьте:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567
TELEGRAM_BOT_USERNAME=sql_hero_bot
```

---

## Шаг 2: Настройка Web App

### 2.1 Development (локальная разработка)

Для локальной разработки используйте **ngrok** для создания публичного URL.

#### Установка ngrok

```bash
# Через npm
npm install -g ngrok

# Или скачать с https://ngrok.com/download
```

#### Запуск ngrok

```bash
# Запустить туннель для frontend
ngrok http 5173
```

Вы получите URL вида:
```
Forwarding   https://abcd1234.ngrok.io -> http://localhost:5173
```

#### Настройка Menu Button

Отправьте BotFather:
```
/setmenubutton
```

Выберите вашего бота, затем:
- **Button text:** `Начать обучение` или `Учить SQL`
- **URL:** `https://abcd1234.ngrok.io` (ваш ngrok URL)

#### Альтернатива ngrok: localtunnel

```bash
npm install -g localtunnel
lt --port 5173
```

### 2.2 Production

Для production используйте ваш реальный домен.

#### Настройка Menu Button

```
/setmenubutton
```

- **Button text:** `Начать обучение`
- **URL:** `https://sqlhero.app` (ваш production домен)

---

## Шаг 3: Настройка домена (Production)

### 3.1 Установить домен

Отправьте BotFather:
```
/setdomain
```

Выберите вашего бота и введите домен:
```
https://sqlhero.app
```

или
```
https://your-app.vercel.app
```

**❗ Требования к домену:**
- Должен использовать HTTPS
- Должен быть доступен публично
- Рекомендуется использовать CDN (Vercel, Netlify, Cloudflare)

### 3.2 Проверка домена

Telegram проверит, что:
1. Домен доступен по HTTPS
2. Сертификат SSL валиден
3. Страница загружается без ошибок

---

## Шаг 4: Описание бота

### 4.1 Краткое описание (About)

Отправьте:
```
/setabouttext
```

Выберите бота и введите:
```
Изучай SQL в игровой форме! 🎮

Интерактивные уроки, практические задания, достижения и таблица лидеров. Начни свой путь в мир баз данных с SQL Hero!
```

**Лимит:** 120 символов

### 4.2 Полное описание (Description)

Отправьте:
```
/setdescription
```

Выберите бота и введите:
```
🎓 SQL Hero — интерактивная платформа для изучения MySQL

📚 Что внутри:
• 10 модулей от основ до продвинутых тем
• 100+ уроков с теорией и практикой
• Интерактивный SQL-редактор
• Песочница для безопасного выполнения запросов

🎮 Геймификация:
• Система опыта и уровней
• 50+ достижений
• Серии дней для мотивации
• Таблица лидеров

🚀 Начни учиться SQL прямо в Telegram!
```

**Лимит:** 512 символов

---

## Шаг 5: Изображения

### 5.1 Фото профиля

Отправьте:
```
/setuserpic
```

Выберите бота и загрузите изображение:
- **Формат:** JPG или PNG
- **Размер:** рекомендуется 512x512px
- **Содержание:** Логотип SQL Hero или тематическое изображение

### 5.2 Bot Picture (новая функция)

Отправьте:
```
/setbotpic
```

Загрузите квадратное изображение для отображения в результатах поиска.

---

## Шаг 6: Команды бота (опционально)

### 6.1 Установить список команд

Отправьте:
```
/setcommands
```

Выберите бота и введите команды (по одной на строку):
```
start - Начать обучение
help - Помощь
stats - Моя статистика
leaderboard - Таблица лидеров
achievements - Мои достижения
```

### 6.2 Как использовать команды

Пользователи смогут:
- Увидеть список команд через кнопку меню
- Использовать автодополнение при вводе `/`
- Быстро получить доступ к функциям

---

## Шаг 7: Дополнительные настройки

### 7.1 Inline Mode (опционально)

Если хотите поддержку inline запросов:
```
/setinline
```

Введите placeholder текст:
```
Найти урок по SQL...
```

### 7.2 Join Request (группы)

Если планируете использовать бота в группах:
```
/setjoingroups
```

Выберите `Enable` или `Disable`.

### 7.3 Privacy Mode

Для работы в группах:
```
/setprivacy
```

Выберите `Disable` чтобы бот видел все сообщения в группе.

---

## Шаг 8: Тестирование

### 8.1 Проверка бота

1. Найдите вашего бота в Telegram по username
2. Нажмите "Start"
3. Нажмите на кнопку меню "Начать обучение"
4. Приложение должно открыться в WebApp

### 8.2 Проверка авторизации

При запуске приложения в Telegram:
1. Backend получит `initData` от Telegram
2. Проверит подпись HMAC
3. Создаст/обновит пользователя
4. Выдаст JWT токен
5. Пользователь будет авторизован

### 8.3 Debug режим

Для отладки добавьте в URL параметр:
```
https://your-domain.com?debug=true
```

Это покажет дополнительную информацию в консоли браузера.

---

## Production Checklist

Перед запуском в production убедитесь:

- [ ] Bot Token сохранён в `.env` и не закоммичен в Git
- [ ] Frontend задеплоен на HTTPS домене
- [ ] Backend задеплоен и доступен
- [ ] Домен настроен в BotFather (`/setdomain`)
- [ ] Menu Button настроена на production URL
- [ ] Описание бота заполнено
- [ ] Фото профиля загружено
- [ ] Команды настроены (опционально)
- [ ] Бот протестирован в Telegram
- [ ] JWT_SECRET_KEY уникальный и безопасный
- [ ] CORS настроен для production домена

---

## Troubleshooting

### Проблема: "Invalid initData signature"

**Причина:** Неверный TELEGRAM_BOT_TOKEN в backend/.env

**Решение:**
1. Проверьте токен в `.env`
2. Убедитесь, что нет лишних пробелов
3. Перезапустите backend
4. Очистите кэш браузера

### Проблема: "Bot not found"

**Причина:** Неверный username бота

**Решение:**
1. Проверьте `TELEGRAM_BOT_USERNAME` в `.env`
2. Убедитесь, что username совпадает с BotFather
3. Username не должен включать `@`

### Проблема: "WebApp failed to load"

**Причина:** Проблемы с доменом или HTTPS

**Решение:**
1. Проверьте, что frontend доступен по HTTPS
2. Проверьте SSL сертификат
3. Проверьте CORS настройки в backend
4. Проверьте логи backend

### Проблема: "Opened outside Telegram"

**Причина:** Приложение открыто напрямую в браузере

**Решение:**
- Это нормально для разработки
- В production открывайте только через Telegram
- Можно добавить проверку `window.Telegram.WebApp.initData`

---

## Безопасность

### Защита Bot Token

1. **Никогда не коммитьте токен в Git**
   ```bash
   # Добавьте в .gitignore
   backend/.env
   frontend/.env
   ```

2. **Используйте переменные окружения**
   ```bash
   # В .env
   TELEGRAM_BOT_TOKEN=your_token_here
   
   # В коде
   token = os.getenv("TELEGRAM_BOT_TOKEN")
   ```

3. **Ротация токенов**
   Если токен утёк, отправьте BotFather:
   ```
   /revoke
   ```
   Выберите бота и подтвердите. Получите новый токен.

### Проверка initData

Backend **обязательно** должен проверять подпись `initData`:

```python
# В app/core/security.py
def verify_telegram_init_data(init_data: str, bot_token: str) -> bool:
    # HMAC SHA-256 проверка
    # Реализовано в проекте
```

**❗ Никогда не доверяйте данным от клиента без проверки!**

---

## Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram WebApp API](https://core.telegram.org/bots/webapps)
- [BotFather Commands](https://core.telegram.org/bots#botfather)
- [ngrok Documentation](https://ngrok.com/docs)

---

## Следующие шаги

- [TESTING.md](TESTING.md) - Тестирование интеграции
- [DEPLOYMENT.md](DEPLOYMENT.md) - Production деплой
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

---

**Успешной настройки бота! 🤖**
